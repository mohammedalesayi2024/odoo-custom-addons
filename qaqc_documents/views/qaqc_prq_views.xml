<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_qaqc_prq_form" model="ir.ui.view">
        <field name="name">qaqc.prq.form</field>
        <field name="model">qaqc.prq</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_submit" string="Submit" type="object"
                            invisible="state != 'draft'" class="oe_highlight"/>
                    <button name="action_start_review" string="Start Review" type="object"
                            invisible="state != 'submitted'"/>
                    <button name="action_approve" string="Approve" type="object"
                            invisible="state not in ('submitted','under_review')" class="oe_highlight"/>
                    <button name="action_reject" string="Reject" type="object"
                            invisible="state not in ('submitted','under_review')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,under_review,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="project_id"/>
                            <field name="supplier_id"/>
                            <field name="discipline"/>
                        </group>
                        <group>
                            <field name="date"/>
                            <field name="rev"/>
                            <field name="consultant_rep_id"/>
                        </group>
                    </group>
                    <group string="Scope">
                        <field name="scope_description" nolabel="1"/>
                    </group>
                    <notebook>
                        <page string="Attachments">
                            <group>
                                <field name="company_profile" filename="company_profile_fname"/>
                                <field name="company_profile_fname" invisible="1"/>
                                <field name="financial_statement" filename="financial_statement_fname"/>
                                <field name="financial_statement_fname" invisible="1"/>
                                <field name="previous_projects" filename="previous_projects_fname"/>
                                <field name="previous_projects_fname" invisible="1"/>
                                <field name="iso_certificates" filename="iso_certificates_fname"/>
                                <field name="iso_certificates_fname" invisible="1"/>
                            </group>
                        </page>
                        <page string="Consultant Decision">
                            <group>
                                <field name="consultant_comments"/>
                                <field name="rejection_reason" invisible="state != 'rejected'"/>
                                <field name="approval_date" readonly="1"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_qaqc_prq_list" model="ir.ui.view">
        <field name="name">qaqc.prq.list</field>
        <field name="model">qaqc.prq</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="project_id"/>
                <field name="supplier_id"/>
                <field name="discipline"/>
                <field name="date"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'approved'"
                       decoration-danger="state == 'rejected'"
                       decoration-info="state in ('submitted','under_review')"/>
            </list>
        </field>
    </record>

    <record id="action_qaqc_prq" model="ir.actions.act_window">
        <field name="name">PRQ - Pre-Qualification</field>
        <field name="res_model">qaqc.prq</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Pre-Qualification request
            </p>
        </field>
    </record>

</odoo>
